[package]
name = "opengauss_encrypt_decrypt"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
opengauss_bindgen = "0.1.0"
magic-crypt = "3.1"

[lib]
crate-type = ["cdylib"]
