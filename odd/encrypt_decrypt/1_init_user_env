gsql -d opengauss -e -c "CREATE TABLE users (
    username TEXT PRIMARY KEY,
    email TEXT,
    passwd TEXT
);"

gsql -d opengauss -e -c "CREATE TABLE secrets(secret text);
INSERT INTO secrets VALUES ('s3cretk3y');"

